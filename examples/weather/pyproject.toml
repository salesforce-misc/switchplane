[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "weather"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "switchplane",
    "httpx",
]

[project.scripts]
weather = "weather.app:main"
