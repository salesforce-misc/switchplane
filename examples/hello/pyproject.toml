[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hello"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "switchplane",
]

[project.scripts]
hello = "hello.app:main"
